import { updateCorrectiveActionAction } from "@/app/actions/corrective-actions";
export function CorrectiveActionForm({ action }: { action: { id: string; status: string; evidenceNote: string | null } }) { return <form action={updateCorrectiveActionAction} className="action-update-form"><input type="hidden" name="actionId" value={action.id} /><select name="status" defaultValue={action.status}><option value="OPEN">Ouverte</option><option value="IN_PROGRESS">En cours</option><option value="BLOCKED">Bloquée</option><option value="COMPLETED">Terminée</option></select><textarea name="evidenceNote" defaultValue={action.evidenceNote ?? ""} placeholder="Preuve de réalisation, lien ou note de suivi" /><button className="secondary-button" type="submit">Mettre à jour</button></form>; }
